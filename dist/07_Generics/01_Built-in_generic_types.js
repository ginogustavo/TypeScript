"use strict";
const names0 = ["Max", "Gino"];
const names = [];
const names2 = [];
const names3 = [];
names[0].split(" ");
const promise = new Promise((resolve, reject) => {
    setTimeout(() => {
        resolve(10);
    }, 2000);
});
promise.then((data) => {
});
